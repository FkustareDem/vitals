# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.4.1] - 修复毛玻璃闪烁

### 修复
- **每几秒闪一下（亮底背景尤其明显）**：毛玻璃原来是"藏窗口 → `BitBlt` 抓桌面 → 自己模糊"，
  每 2 秒重抓一次，那个"藏一下"就会被看到。改为 **DWM 亚克力实时模糊**
  （`SetWindowCompositionAttribute` + `ACCENT_ENABLE_ACRYLICBLURBEHIND`），
  模糊由系统合成器实时完成 —— **零抓屏、零延迟、零闪烁**。
  系统不支持亚克力时才退回静态抓屏，`--bench` 会显示当前用的是哪种。
- 移除每 2 秒的周期抓屏，桌面内容变化不再需要任何重抓

## [1.4.0] - 圆角背景

### 新增
- `bg_style`：**黑灰渐变 / 毛玻璃 / 通透玻璃** 三种圆角背景
- `bg_radius`：圆角半径，会被钳到 `窗口高/2`，给大值就是**四分之一个圆的胶囊端**
- `bg_alpha`：背景透明度，未设置时按风格给默认值
- 设置界面「外观」页新增对应三项控件
- 毛玻璃真的抓取窗口后面的桌面并模糊（`BitBlt` + `DwmFlush` + 两趟盒式模糊），每 2 秒刷新；抓失败会自动退回渐变
- 圆角遮罩用 4×4 超采样，边缘抗锯齿；滚动文字一起被裁成胶囊形
- 新增看门线程：菜单/拖窗等**模态循环**期间用 `PostMessage` 驱动帧（`WM_TIMER` 会被模态循环大量丢弃）

## [1.2.0] - 波形可调
### 新增
- `wave_bold`：波形粗细 0~4（实测平均笔画宽度 1.21 / 2.44 / 3.66 / 4.89 px）

## [1.1.0] - 内置默认配置 + 帧率校准
### 新增
- exe **内置默认 `vitals.ini`**，第一次运行自动释放 —— 只发一个 exe 也能用
### 修复
- **右键菜单期间严重掉帧**：`WaitForSingleObject(hTimer, 0)` 探测会吃掉**自动复位**定时器的信号 → 改用**手动复位**定时器
- 帧率从 58.8 校准到 **59.97fps**：改成直接估计定时器唤醒延迟（外生量），而不是用周期误差（纯积分环会自我抵消）

## [1.0.0] - 波形抗锯齿 + 帧节奏
### 修复
- **QRS "蹦迪"**：对 1 像素宽的尖峰做纵向插值会削幅度 → 改成**横向 4 倍过采样 + 子列覆盖度平均**
- **波形顿挫**：`ScheduleFrame` 从「跨帧漂移补偿」改成「帧内补偿」（等待 = 周期 − 本帧耗时），帧间隔标准差 **1.57ms → 0.167ms**
- 帧率被误设成 30 的配置问题

## [0.9.0] - 波形 min/max 抽取
### 修复
- **波形线宽忽粗忽细**：对整条波形做「取离基线最远的采样」是错的，平滑段会变成最小/最大**包络**。改成标准做法 **min/max 抽取**

## [0.8.0] - ini 与滚动
### 修复
- **`vitals.ini` 被写成"一行天书"**：文本模式下 CRT 把 `\n` 翻成 `\r\n`，而文件本来就是 `\r\n` → 每保存一次多一层 CR（实测积累到 24469 个）
- 模板支持 `line1` / `line2` … 分段写，程序自动拼成一行
- 文字改用亚像素插值消除整数像素的周期性顿挫

## [0.7.0] - 帧率
### 新增
- 高精度等待定时器 + `MsgWaitForMultipleObjectsEx` 自建游戏式主循环
- `--bench` / `--selftest` 诊断工具

## [0.5.0] - 首个可用版本
### 新增
- 透明跑马灯、23 个数值槽、每槽独立刷新节奏、超标箭头
- ECG / PPG / RESP 波形，节律跟随 HR / RR
- 托盘图标、右键菜单、开机自启动、设置界面、28 台监测设备选择
- 覆盖度图染色（让 GDI 也能画带抗锯齿的彩色透明文字）
- 固定像素格子对齐（滚动不抖）
